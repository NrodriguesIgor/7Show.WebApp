
import { Presenter } from '../../shared/model/Presenter.model';
export class Show {
    showId: string;
    name: string;
    description: string;
    obs: string;
    code: string;
    presenter: Presenter;
}